// scripts.js

document.addEventListener('DOMContentLoaded', () => {
    const button = document.querySelector('.hero-content button');
    button.addEventListener('click', () => {
        alert('Välkommen att utforska våra sekelskifteshus!');
    });
});
